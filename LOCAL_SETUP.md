# 🖥️ Настройка для локальной разработки (Windows)

## Проблема

При запуске приложения локально на Windows вы получаете ошибку:
```
Error: getaddrinfo ENOTFOUND kaiten.alfastrah.ru
```

Это происходит потому, что **секреты Replit недоступны при локальной работе**.

## ✅ Решение

### Шаг 1: Создайте файл `.env` в корне проекта

В корне проекта (там же, где `package.json`) создайте файл с именем `.env` (без расширения).

**Windows:**
1. Откройте проводник
2. Перейдите в папку проекта
3. Создайте новый текстовый файл
4. Назовите его `.env` (точка в начале обязательна!)
5. Windows может предупредить, что файл без расширения - это нормально

Или через командную строку:
```cmd
cd путь\к\проекту
type nul > .env
```

### Шаг 2: Заполните `.env` файл

Откройте `.env` в любом текстовом редакторе (Notepad++, VS Code, Sublime) и вставьте:

```env
# AI Configuration для PDF отчетов
OPENAI_API_KEY=ваш-openai-api-key-если-нужны-отчеты

# AI Model (опционально, по умолчанию gpt-4o-mini)
AI_MODEL=gpt-4o-mini

# Database (ваша ЛОКАЛЬНАЯ база данных)
DATABASE_URL=postgresql://postgres:ваш_пароль@localhost:5432/ваша_база_данных

# Session Secret (любая случайная строка)
SESSION_SECRET=any-random-secret-string-for-local-development

# 🔴 САМОЕ ВАЖНОЕ: Kaiten Integration
KAITEN_DOMAIN=kaiten.alfastrah.ru
KAITEN_API_KEY=ваш-kaiten-api-key

# Kaiten для фронтенда (должен совпадать с KAITEN_DOMAIN)
VITE_KAITEN_DOMAIN=kaiten.alfastrah.ru
```

### Шаг 3: Замените значения на реальные

**Обязательно замените:**

1. **KAITEN_DOMAIN** и **VITE_KAITEN_DOMAIN**:
   - Если ваш Kaiten на `kaiten.alfastrah.ru` - оставьте как есть
   - Если на другом домене - замените

2. **KAITEN_API_KEY**:
   - Получите API ключ в вашем Kaiten
   - Вставьте сюда

3. **DATABASE_URL**:
   - Замените на строку подключения к вашей локальной PostgreSQL
   - Пример: `postgresql://postgres:mypassword@localhost:5432/initiatives_db`

4. **OPENAI_API_KEY** (опционально):
   - Нужен только если будете генерировать PDF отчеты с AI
   - Можно оставить пустым, если не нужны AI отчеты

### Шаг 4: Перезапустите приложение

```bash
# Остановите текущий процесс (Ctrl+C)
# Запустите снова:
npx cross-env NODE_ENV=development tsx server/index.ts
```

## 🔒 Безопасность

**ВАЖНО:**
- Файл `.env` уже добавлен в `.gitignore`
- **НИКОГДА** не коммитьте `.env` в Git
- **НЕ ПУБЛИКУЙТЕ** API ключи в открытом доступе

## ✅ Проверка

После создания `.env` файла:

1. Перезапустите сервер
2. Откройте приложение в браузере
3. Попробуйте синхронизировать команду с Kaiten
4. Если всё ок - синхронизация пройдет без ошибки `ENOTFOUND`

## 📋 Пример готового .env файла

```env
# AI Configuration
OPENAI_API_KEY=sk-proj-abc123xyz...

# Database
DATABASE_URL=postgresql://postgres:mypass@localhost:5432/initiatives

# Session
SESSION_SECRET=my-super-secret-key-12345

# Kaiten
KAITEN_DOMAIN=kaiten.alfastrah.ru
KAITEN_API_KEY=your-kaiten-api-key-here
VITE_KAITEN_DOMAIN=kaiten.alfastrah.ru
```

## 🆘 Если всё еще не работает

Проверьте:
1. ✅ Файл называется `.env` (точка в начале)
2. ✅ Файл находится в корне проекта (рядом с `package.json`)
3. ✅ Нет пробелов вокруг знака `=` в файле
4. ✅ Домен Kaiten корректный и доступен из вашей сети
5. ✅ API ключ Kaiten валидный
6. ✅ Перезапустили сервер после создания `.env`

## 🌐 Проверка доступности Kaiten

Проверьте, доступен ли домен:

```bash
# Через браузер
https://kaiten.alfastrah.ru

# Через командную строку
ping kaiten.alfastrah.ru
```

Если ping не проходит - возможно, домен недоступен из вашей сети (firewall, VPN, и т.д.).
