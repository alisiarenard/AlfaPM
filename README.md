# AlfaPM - Система управления инициативами и спринтами

Веб-приложение для визуализации и отслеживания инициатив команд по спринтам с интеграцией Kaiten API и возможностью формирования Excel отчетов.

## Возможности

### Основные функции
- 📊 **Визуализация инициатив** - наглядное отображение инициатив команд по спринтам
- 🔄 **Двусторонняя синхронизация с Kaiten** - автоматическая синхронизация данных с Kaiten API
- 📈 **Метрики и аналитика** - Innovation Rate, Value/Cost, структура затрат
- 📥 **Excel отчеты** - формирование детальных отчетов с полным стилевым оформлением
- ✏️ **Inline редактирование** - редактирование данных прямо в таблице
- 🔗 **Ссылки на Kaiten** - быстрый переход к задачам в Kaiten
- 🌐 **URL-роутинг** - сохранение фильтров в URL для возможности делиться ссылками

### Визуализация данных
- Таймлайн инициатив с прогнозируемыми датами
- Индикаторы статуса и завершенности инициатив
- Прогресс-бары для размера, затрат, эффекта и Value/Cost
- Автоматическая прокрутка к предыдущему спринту
- Поддержка нескольких команд и департаментов

### Excel отчеты (ExcelJS)
- **Лист "Структура затрат"**: разбивка по типам инициатив и задач
- **Лист "Инициативы"**: детальная информация с группировкой и стилизацией
- Полное форматирование: шрифты, цвета, выравнивание, числовые форматы
- Автоматический расчет Value/Cost для разных типов инициатив

## Технологический стек

### Frontend
- React 18+ с TypeScript
- Vite - сборка и инструменты разработки
- Wouter - роутинг
- TanStack React Query - управление состоянием сервера
- Shadcn/ui + Radix UI - UI компоненты
- Tailwind CSS - стилизация
- ExcelJS - формирование Excel отчетов
- Lucide React - иконки

### Backend
- Node.js + Express
- TypeScript + ESM
- PostgreSQL (Neon) - база данных
- Drizzle ORM - работа с БД
- Kaiten API - интеграция с системой управления задачами

## Установка и запуск

### Требования
- Node.js 18+
- PostgreSQL 14+
- npm или yarn

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/alisiarenard/AlfaPM.git
cd AlfaPM
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

### Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# База данных PostgreSQL
DATABASE_URL=postgresql://username:password@localhost:5432/database_name
PGHOST=localhost
PGPORT=5432
PGUSER=your_username
PGPASSWORD=your_password
PGDATABASE=your_database_name

# Секрет для сессий
SESSION_SECRET=your_random_secret_here

# Kaiten API
KAITEN_API_KEY=your_kaiten_api_key
KAITEN_DOMAIN=your_company.kaiten.ru
```

### Шаг 4: Инициализация базы данных

```bash
npm run db:push
```

Эта команда создаст все необходимые таблицы в базе данных.

### Шаг 5: Запуск приложения

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5000`

## Структура проекта

```
AlfaPM/
├── client/              # Frontend (React + TypeScript)
│   ├── src/
│   │   ├── components/  # UI компоненты
│   │   ├── pages/       # Страницы приложения
│   │   ├── lib/         # Утилиты и хелперы
│   │   └── hooks/       # React hooks
├── server/              # Backend (Express + TypeScript)
│   ├── routes.ts        # API эндпоинты
│   ├── storage.ts       # Интерфейс работы с БД
│   └── init-db.ts       # Инициализация БД
├── shared/              # Общий код
│   └── schema.ts        # Drizzle схема БД
└── package.json
```

## API Эндпоинты

### Департаменты
- `GET /api/departments` - получить все департаменты
- `POST /api/departments` - создать департамент
- `PATCH /api/departments/:id` - обновить департамент
- `DELETE /api/departments/:id` - удалить департамент

### Команды
- `GET /api/teams` - получить все команды
- `POST /api/teams` - создать команду
- `PATCH /api/teams/:id` - обновить команду
- `DELETE /api/teams/:id` - удалить команду

### Инициативы
- `GET /api/initiatives` - получить инициативы с фильтрами
- `PATCH /api/initiatives/:cardId` - обновить инициативу

### Задачи
- `GET /api/tasks` - получить задачи
- `GET /api/tasks/by-sprint/:sprintId` - задачи по спринту

### Метрики
- `GET /api/metrics/innovation-rate` - Innovation Rate
- `GET /api/metrics/cost-structure` - структура затрат
- `GET /api/metrics/value-cost` - метрики Value/Cost

### Синхронизация Kaiten
- `POST /api/kaiten/sync/:boardId` - синхронизация доски
- `PATCH /api/kaiten/update-initiative/:cardId` - обновление инициативы

## Особенности работы

### Расчет метрик

**Innovation Rate**: процент story points из задач, связанных с Epic, Compliance или Enabler инициативами

**Value/Cost**:
- Для Epic: рассчитывается на основе плановых и фактических значений
- Для Compliance и Enabler: автоматически равен 1.0 (значение = затратам)

**Cost Structure**: разбивка story points по типам инициатив/задач

### Синхронизация с Kaiten

Приложение синхронизирует:
- Инициативы (карточки с типом Epic, Compliance, Enabler)
- Задачи и их story points
- Статусы и даты
- Кастомные поля (planned_value, fact_value)

### Фильтрация по URL

Все фильтры сохраняются в URL параметрах:
- `dept` - ID департамента
- `year` - год (2025/2026)
- `teams` - выбранные команды (через запятую)
- `active` - показывать только активные
- `tab` - активная вкладка команды

Это позволяет делиться ссылками с заданными фильтрами.

## Скрипты

```bash
npm run dev        # Запуск в режиме разработки
npm run build      # Сборка для продакшена
npm run db:push    # Синхронизация схемы БД
npm run db:studio  # Открыть Drizzle Studio
```

## Лицензия

Этот проект создан для внутреннего использования.

## Автор

Проект разработан для управления инициативами и спринтами команд с использованием современного стека веб-технологий.
